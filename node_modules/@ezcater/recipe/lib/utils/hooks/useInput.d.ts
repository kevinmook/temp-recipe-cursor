import React from 'react';
declare type InputBindings<TValue> = {
    value: TValue;
    onChange: React.ChangeEventHandler;
};
declare const useInput: <TValue>(initialValue: TValue) => InputBindings<TValue>;
export default useInput;
