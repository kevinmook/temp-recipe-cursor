import { Theme } from '../../themes/styled';
declare type Props = {
    horizontal?: boolean;
    theme: Theme;
};
export declare const vertical: ({ horizontal, theme }: Props) => import("@emotion/utils").SerializedStyles;
export declare const horizontal: ({ horizontal: isHorizontal, theme }: Props) => import("@emotion/utils").SerializedStyles;
export {};
