import React from 'react';
declare type SharedButtonProps = {
    destructive?: boolean;
    disabled?: boolean;
    disabledMessage?: string;
    onClick?: React.MouseEventHandler;
};
/**
 * Buttons represent actions on a page that can be triggered with one click.
 * Buttons can be used in forms, or in other locations in a page to communicate that an action is available.
 */
declare const EzButton: React.ForwardRefExoticComponent<(React.ButtonHTMLAttributes<HTMLButtonElement> & {
    loading?: boolean;
    use: "primary" | "secondary";
} & SharedButtonProps & React.RefAttributes<HTMLButtonElement>) | (React.ButtonHTMLAttributes<HTMLButtonElement> & {
    use: "tertiary";
} & SharedButtonProps & React.RefAttributes<HTMLButtonElement>)>;
/**
 * @component
 */
export default EzButton;
