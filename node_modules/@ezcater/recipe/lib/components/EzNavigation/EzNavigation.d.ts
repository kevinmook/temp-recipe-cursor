import { SFC } from 'react';
import { LogoType } from './Logo';
import UserMenu from './UserMenu';
import { LabelledLink } from '../EzLink/EzLink.types';
declare type HomeLink = LabelledLink & {
    logo?: LogoType;
};
declare type NavLink = LabelledLink & {
    notifications?: number | '★';
};
declare type UserMenu = {
    links: LabelledLink[];
    name: string;
};
declare type Props = {
    links: NavLink[];
    utilityLinks?: NavLink[];
    userMenu?: UserMenu;
    home: HomeLink;
};
declare const EzNavigation: SFC<Props>;
export default EzNavigation;
