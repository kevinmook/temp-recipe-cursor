import { SFC } from 'react';
import { LabelledLink, Link as LinkType } from '../EzLink/EzLink.types';
interface LogoProps {
    link: LabelledLink & LinkType;
    logo: {
        src: string;
        width: number | string;
    };
}
declare const Logo: SFC<LogoProps>;
export declare type LogoType = {
    src: string;
    width?: number;
};
export default Logo;
