import { CreateStyled } from '@emotion/styled';
import * as standard from './standard';
export declare type Theme = typeof standard;
export declare type Themed = {
    theme: Theme;
};
declare const _default: CreateStyled<typeof standard>;
export default _default;
